function setup() {
  var cnv =createCapture(VIDEO);;
  var x = (windowWidth - width) / 2;
  var y = (windowHeight - height) / 2;
  cnv.position(x, y).size(200,200);
  
  
}

function draw(){
	
	
	
	}
